---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
  - name: pipeline-source

outputs:
  - name: image-list

run:
  path: pipeline-source/ci/list-images.sh